# Secrets – NIEMALS committen / NEVER commit
.env

# Logs
*.log

# Backoff-/Alarm-Status (Laufzeit) / runtime backoff state
glasfaser_watchdog_state.json

# Python
__pycache__/
*.pyc
*.pyo

# Debug-Screenshots
*.png

# Editor
.vscode/
.idea/
*.swp
*~

# Forensik: Screenshots, DOM-Kopien, Playwright-Traces und WAN-Config-Backups.
# Die Config-Backups enthalten die komplette Router-Konfiguration – Passwortfelder
# sind zwar geschwaerzt, aber Benutzername und Anschlusskennung stehen drin.
forensik/
*.html
*.zip
wan-config/
glasfaser_watchdog_disabled

# Private Notizen – gehoeren nicht ins oeffentliche Repo
1und1_wan_settings.md

# Werkzeug-Einstellungen: enthalten lokale Pfade mit dem Benutzernamen.
# Nicht versioniert, aber eingetragen, damit ein `git add -A` sie nicht
# versehentlich mitnimmt.
.claude/
